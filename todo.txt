20211224 :  5:41
20211230 :  4:55
20220217 : 19:01
20220623 :  7:27
20240502 : 10:07
20240506 : 22:30







"the last breaking change" non serialization of a virtual base


-------------------------------------------------------------------------------------------------
-------------------------------------------------------------------------------------------------
-------------------------------------------------------------------------------------------------

private:
   struct IntrusivePtrInput {
       using Deleter = void(*)(void*);
       std::type_index type = typeid(int); // TODO P5: better way to skip initialization, or use ctor args
       void* ptr = nullptr;
       Deleter deleter = nullptr;

       explicit IntrusivePtrInput(int) {} // TODO P5: better way to default ctor with emplace?
       ~IntrusivePtrInput() {
           if (deleter)
               deleter(ptr);
       }
   };
   struct IntrusivePtrOutput {
       using Deleter = void(*)(void*);
       uint32_t ref = 0;
       void* ptr = nullptr;
       Deleter deleter = nullptr;

       explicit IntrusivePtrOutput(int) {} // TODO P5: better way to default ctor with emplace?
       ~IntrusivePtrOutput() {
           if (deleter)
               deleter(ptr);
       }
   };
public:
   std::unordered_map<uint32_t, IntrusivePtrInput> inputIntrusivePtrLookup;
   std::unordered_map<void*, IntrusivePtrOutput> outputIntrusivePtrLookup;

-------------------------------------------------------------------------------------------------

// TODO P1: Move intrusive_ptr serialization support to somewhere more generic, provide better archive support for
//         any smart pointer
// TODO P1: Polymorphic type support for intrusive_ptr
// --- libv::intrusive_ptr
template <typename Archive, typename T>
inline void load(Archive& ar, libv::intrusive_ptr<T>& var) {
   uint32_t ref = 0;
   ar.nvp("ref", ref);

   if (ref == 0) {
       var = nullptr;
       return;
   }

   const auto [it, isNew] = ar.inputIntrusivePtrLookup.emplace(ref, 0);
   if (isNew) {
       // TODO P2: Use libv::vide::access to create default object (?)
       auto ptr = libv::make_intrusive_ptr<std::remove_const_t<T>>();
       it->second.type = typeid(T);
       it->second.ptr = ptr.ptr();
       it->second.deleter = +[](void* ptr) {
           // Use the destructor of the intrusive_ptr to clean up by creating/reattaching the detached pointer
           // onto a stack variable
           auto tmp = libv::intrusive_ptr<T>(static_cast<T*>(ptr), typename libv::intrusive_ptr<T>::no_increment_tag{});
           (void) tmp;
           // tmp's destructor cleans up the ref_count
       };
       ar.nvp("data", *ptr);
       (void) ptr.detach(); // Detach the pointer to keep it alive, its ptr and its deleter was already saved

   } else if (it->second.type != typeid(T)) {
       throw vide::Exception("Error while trying to deserialize a smart pointer. Type mismatch find in ref " + std::to_string(ref));
   }

   var = libv::intrusive_ptr<T>(static_cast<T*>(it->second.ptr));
}

-------------------------------------------------------------------------------------------------

template <typename Archive, typename T>
inline void save(Archive& ar, const libv::intrusive_ptr<T>& var) {
   const auto [it, isNew] = ar.outputIntrusivePtrLookup.emplace(var.ptr(), 0);

   if (!isNew) {
       ar.nvp("ref", it->second.ref);
   } else {
       // Copy of shared pointers placed in outputIntrusivePtrLookup to make sure they are kept alive
       //  during lifetime of the serialization to prevent CVE-2020-11105
       auto ptr = var;
       it->second.ref = static_cast<uint32_t>(ar.outputIntrusivePtrLookup.size());
       it->second.ptr = ptr.ptr();
       it->second.deleter = +[](void* ptr) {
           // Use the destructor of the intrusive_ptr to clean up by creating/reattaching the detached pointer
           // onto a stack variable
           auto tmp = libv::intrusive_ptr<T>(static_cast<T*>(ptr), typename libv::intrusive_ptr<T>::no_increment_tag{});
           (void) tmp;
           // tmp's destructor cleans up the ref_count
       };
       ar.nvp("ref", it->second.ref);
       ar.nvp("data", *var);
       (void) ptr.detach(); // Detach the pointer to keep it alive, its ptr and its deleter was already saved
   }
}

-------------------------------------------------------------------------------------------------
-------------------------------------------------------------------------------------------------
-------------------------------------------------------------------------------------------------

std::string hex_dump_with_ascii(const std::string_view& s) {
	if (s.empty())
		return "";

	std::ostringstream ss;
	ss << std::hex << std::setfill('0') << std::setw(2) << std::uppercase;

	std::size_t i = 0;
	while (true) {
		ss << std::setw(2) << uint16_t{static_cast<unsigned char>(s[i])};
		++i;

		if (i == s.size())
			break;

		if (i % 32 == 0) {
			ss << " | ";
			for (std::size_t j = i - 32; j < i;) {
				ss << (std::isprint(s[j]) ? s[j] : '.');
				++j;
				if (j % 8 == 0 && j % 32 != 0)
					ss << ' ';
			}
			ss << "\n";
		} else if (i % 8 == 0) {
			ss << "  ";
		} else {
			ss << ' ';
		}
	}

	if (i % 32 != 0) {
		const auto r_begin = i;
		const auto r_end = (i / 32 + 1) * 32;

		for (std::size_t r = r_begin; r < r_end; ++r) {
			if (r % 32 == 0) {
				ss << " ";
			} else if (r % 8 == 0) {
				ss << "    ";
			} else {
				ss << "   ";
			}
		}
	}

	ss << " | ";
	for (std::size_t j = (i - 1) / 32 * 32; j < i;) {
		ss << (std::isprint(s[j]) ? s[j] : '.');
		++j;
		if (j % 8 == 0 && j % 32 != 0)
			ss << ' ';
	}

	return std::move(ss).str();
}

-------------------------------------------------------------------------------------------------
-------------------------------------------------------------------------------------------------
-------------------------------------------------------------------------------------------------








vide: polymorphic with proxy archives?
	\ only if proxy opt-ins for it

vide: Customization point for smart pointers (libv::intrusive_ptr support)
	\ shared_ptr could/should use this too (granted that would require polymorphic features in it)
	yeah, polymorphic support would be nice for this too

things to break:
	smart pointer data representation

vide: archive.eof() or some API to check if the whole of the buffer was consumed


vide: ar.peek<T>() (load without progressing the input it (binary)/array index (text))

vide: Make sure binary_data does not swap bytes for floating points numbers

vide: Archive.reset() (close archive, and clear every state in the archive, but keep allocated memory, make it reusable)
vide: Archive.finish() or .flush() similar to reset()
vide: archive.binary_data(ptr, size) and archive.binary_data(name, ptr, size) dependent name or function for BinaryData, cleanup BinaryData
vide: archive.enable_trace(): detailed exception (try-catch and append debug info in every serializer), as this might be heavy it might need to be a compile time flag not runtime

vide: visiting_archive
vide: force inlines

Better document:
	validate_read_size
	safe_to_reserve
	is_binary_archive
	maximumBinaryReadSize

cleanup SizeTag

vide: std::vector<std::byte> should be base64 serialized in JSON/XML
	(any std::ranges::contiguous_range of std::byte)



Merge/Implement the following PRs:
	https://github.com/USCiLab/cereal/pull/641 Use unordered containers and remove std::function from polymorphic_impl.hpp

Think about it:
	https://github.com/USCiLab/cereal/pull/321 Json/XML Archive improvement (basically only the stack info is relevant improvement)
